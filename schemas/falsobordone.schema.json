{
  "$id": "falsobordone",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/Falsobordone",
  "description": "Record of a falsobordone in music document.",
  "$comment": "Move some defs to $def?",
  "readOnly": true,
  "title": "Record – falsobordone.",
  "definitions": {
    "Document": {
      "title": "Document",
      "description": "Document or artifact containing the described falsobordone.",
      "type": "object",
      "properties": {
        "id": {
          "title": "Reference ID",
          "description": "Name by which this document is referenced within the database.",
          "examples": ["#LermaCodex"],
          "type": "string",
          "pattern": "#.+"
        },
        "name": {
          "title": "Name",
          "description": "'Friendly' name by which the source is often referred to",
          "examples": ["Lerma Codex"],
          "type": "string"
        },
        "shelfbook": {
          "$comment": "TO DO: more professional description!",
          "title": "Shelfbook record",
          "description": "Library reference number",
          "examples": ["NL-Uu Hs.3 L16"],
          "type": "string"
        },
        "medium": {
          "title": "Medium",
          "description": "By what means the document was produced.",
          "enum": [
            "manuscript",
            "print of one type",
            "print of another type",
            "etc",
            "oral"
          ]
        },
        "category": {
          "title": "Category",
          "description": "What kind of document it is.",
          "enum": ["choirbook", "partbook", "other", "transcription"]
        },
        "links": {
          "title": "Links",
          "description": "Links containings data related to the source.",
          "item": { "$ref": "#/definitions/Link" }
        }
      },
      "required": ["id", "name", "shelfbook", "medium", "category", "links"]
    },
    "Tonus": {
      "title": "Tonus",
      "description": "Information about the tonus.",
      "type": "object",
      "properties": {
        "tonusID": {
          "title": "Tonus ID",
          "description": "Number of tonus (as a Latin number or label).",
          "$comment": "TO DO: move it to another database",
          "examples": ["VIII"],
          "enum": [
            "I",
            "II",
            "III",
            "IV",
            "V",
            "VI",
            "VII",
            "VIII",
            "Peregrinus",
            "Other"
          ]
        },
        "variantID": {
          "title": "Tonus termination variant",
          "description": "Reference to termination variant of the psalmtone.",
          "examples": ["a", "g2"],
          "type": "string"
        },
        "nameInDocument": {
          "title": "Tonus Name in document",
          "description": "How the tonus is described in the document. Note: may contain TEI.",
          "examples": ["Octav<sic>o</sic>s"],
          "type": "string"
        }
      },
      "required": ["tonusID", "variantID", "nameInDocument"]
    },
    "Falsobordone": {
      "title": "Falsobordone",
      "description": "A record containing information about falsobordone.",
      "type": "object",
      "properties": {
        "file": {
          "$comment": "TO DO: URI",
          "title": "File name",
          "description": "MEI file containing falsobordone",
          "type": "string",
          "format": "URI"
        },
        "titleInDocument": {
          "title": "Title (as in document)",
          "description": "How the music is entitled in the source.",
          "examples": ["Fabordon para las chirimias"],
          "type": "string"
        },
        "layout": {
          "title": "Part layout",
          "description": "How are the composition's parts (voices) layed out in the source.",
          "enum": ["choirbook", "partitura", "partbooks"]
        },
        "parts": {
          "title": "Parts",
          "description": "Parts (voices) of the compositions",
          "examples": ["Cantus"],
          "type": "array",
          "items": { "$ref": "#/definitions/Part", "minItems": 1 }
        },
        "analysis": {
          "$ref": "#/definitions/Analysis"
        }
      },
      "required": ["file", "titleInDocument", "layout", "parts", "analysis"]
    },
    "Analysis": {
      "title": "Analysis",
      "description": "Data derived from the transcribed source (but not necessarily contained in (or directly derivable from) the MEI file, from which they cannot be trivially derived. Per hemistich.",
      "type": "object",
      "properties": {
        "cantusFirmus": {
          "$comment": "TO DO: analyze hemistichs",
          "title": "Cantus Firmus",
          "description": "How is cantus firmus treated in the composition",
          "type": "object",
          "properties": {
            "part": {
              "title": "Part label (standardized)",
              "description": "Which part is it (note: the original voice labeling should be provided within the MEI file.",
              "$ref": "#/definitions/PartID"
            },
            "tonus": {
              "title": "Tonus",
              "$ref": "#/definitions/Tonus"
            }
          },
          "required": ["part", "tonus"]
        },
        "initHemistich": {
          "title": "Initial Hemistich",
          "description": "Initial hemistich",
          "$ref": "#/definitions/TonalContent"
        },
        "finalHemistich": {
          "title": "Concluding Hemistich",
          "description": "Concluding hemistich",
          "$ref": "#/definitions/TonalContent"
        }
      },
      "required": ["cantusFirmus", "initHemistich", "finalHemistich"]
    },

    "TonalContent": {
      "title": "Part (Voice) Tonal Content",
      "description": "Information about the tonal content of a part (voice).",
      "type": "object",
      "properties": {
        "intonatio": {
          "title": "Intonatio",
          "description": "Very first written pitch of the part.",
          "examples": ["G"],
          "$ref": "#/definitions/PitchID"
        },
        "tenor": {
          "title": "Recit pitch (tenor)",
          "description": "The written pitch corresponding to cantus firmus's tenor (reciting) pitch.",
          "examples": ["C"],
          "$ref": "#/definitions/PitchID"
        },
        "mediatio": {
          "title": "Mediatio",
          "description": "The pitch concluding mediation.",
          "examples": ["A"],
          "$ref": "#/definitions/PitchID"
        },
        "terminatio": {
          "title": "Final pitch (terminatio)",
          "description": "The written pitch corresponding to cantus firmus's last note of terminatio",
          "examples": ["G"],
          "$ref": "#/definitions/PitchID"
        }
      },
      "required": ["tenor"]
    },

    "TonalOutline": {
      "title": "Tonal Outline",
      "description": "Information about the tonal outline of Falsobordone. Note: if cf is in bass, fill only \"tenor\".",
      "type": "object",
      "properties": {
        "tenor": { "$ref": "#/definitions/TonalContent" },
        "bass": {
          "$ref": "#/definitions/TonalContent",
          "title": "Bass (structural)",
          "description": "Information about the lowest structural notes."
        }
      },
      "required": ["tenor"]
    },

    "Part": {
      "title": "Part (or voice)",
      "description": "A description of a single part of a composition.",
      "type": "object",
      "properties": {
        "id": { "$ref": "#/definitions/PartID" },
        "document": {
          "title": "Document",
          "description": "Reference to a document definition.",
          "example": "#LermaCodex (if exists)",
          "type": "string",
          "pattern": "#.+"
        },
        "nameInDocument": {
          "title": "Part name (in document)",
          "description": "How part is named within the document. Can contain TEI.",
          "examples": ["Teno<>r</>"],
          "type": "string"
        },
        "folio": {
          "title": "Folio",
          "description": "On which folio(s) the part is written.",
          "example": ["41-42v, 42, 51v-52"],
          "type": "string",
          "pattenrn": "\\d+(v?-\\d+v?)?(, \\d+(v?-\\d+v?)?)*"
        },
        "staff": {
          "title": "Staff number annotation",
          "description": "On which folio's staff the voice is written (counting from 1) OR the staff closest to preceeding part position. Note: empty staves also count.",
          "example": [4],
          "type": "number",
          "exclusiveMinimum": 0
        },
        "staffAnnotation": {
          "title": "Staff position comment",
          "description": "If the part is located on a staff in such a way that providing an exact staff number is problematic, provide a comment.",
          "examples": [
            "An additional handwritten staff between 3rd and 4th staff on the print's page. [staff: 3]",
            "An additional handwritten staff on the page margin. [staff: 3]"
          ],
          "type": "string"
        },
        "links": {
          "$ref": "#/definitions/Link",
          "title": "Links",
          "description": "Links containings data related to the source.",
          "item": {
            "$comment": "TO DO: make this schema 'extend' Link schema such that it has compulsory type 'image', 'IFFF' or 'audio' (?) for linkType property.",
            "$ref": "#/definitions/Link"
          }
        }
      },
      "required": ["id", "document", "folio", "staff", "links"]
    },

    "PartID": {
      "title": "Standard part name",
      "description": "A typical name of this part.",
      "$comment": "TO DO: use some more formal convention.",
      "enum": ["cantus", "altus", "tenor", "bassus"]
    },

    "PitchID": {
      "title": "Pitch ID",
      "description": "A letter name of a pitch.",
      "examples": ["A", "Bb", "F#"],
      "type": "string",
      "pattern": "[A-G](b*|#*)"
    },

    "Link": {
      "title": "Link",
      "description": "A link to the source.",
      "type": "object",
      "properties": {
        "linkType": {
          "title": "Type",
          "description": "Type of the linked source.",
          "enum": [
            "catalogue",
            "reference",
            "image",
            "IFFF",
            "audio",
            "edition"
          ]
        },
        "href": {
          "title": "Link",
          "description": "Link.",
          "type": "string",
          "format": "url"
        },
        "annotation": {
          "title": "Annotations",
          "description": "Any remarks about the content within the link.",
          "type": "string"
        }
      }
    }
  }
}
